-- Line-anchored code annotations sent to a Claude Code session in another
-- kitty window. See `nvim/lua/config/annotations/init.lua` for the actions,
-- keymaps, signs, and highlight groups `setup(opts)` accepts.

require("config.annotations").setup()
